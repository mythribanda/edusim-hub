{
  "formulas": ["SI = (P * R * T) / 100"],
  "precision": 2,
  "variables": [
    { "symbol": "P", "label": "Principal Amount", "defaultValue": 1000, "min": 100, "max": 10000, "step": 100, "unit": "$" },
    { "symbol": "R", "label": "Rate of Interest", "defaultValue": 5, "min": 0.5, "max": 15, "step": 0.5, "unit": "%/yr" },
    { "symbol": "T", "label": "Time Period", "defaultValue": 3, "min": 1, "max": 20, "step": 1, "unit": "years" },
    { "symbol": "SI", "label": "Simple Interest", "defaultValue": 0, "min": 0, "max": 30000, "step": 1, "unit": "$" }
  ],
  "tier_rules": {
    "primary": {
      "variables_shown": ["P", "T"],
      "description": "Learn how lending money earns interest over time.",
      "title": "Simple Interest Explorer",
      "show_graph": true
    },
    "middle": {
      "variables_shown": ["T"],
      "description": "Calculate interest earned over a set of years with fixed rate and principal.",
      "title": "Simple Interest Growth",
      "show_graph": true
    },
    "high_school": {
      "variables_shown": ["P", "R", "T"],
      "description": "Examine simple interest accumulations on different principal sums, rates, and terms.",
      "title": "Interest & Finance Lab",
      "show_graph": true
    },
    "university": {
      "description": "Advanced financial sandbox supporting custom interest rate formula configurations.",
      "title": "Finance Sandbox",
      "show_graph": true,
      "custom_formula": true,
      "export_csv": true
    }
  }
}
