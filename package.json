{
  "name": "edusim-hub",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@11.12.1",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "backend": "cd services/api && uvicorn main:app --reload --port 8001 --host 0.0.0.0",
    "dev:backend": "cd services/api && venv\\Scripts\\uvicorn main:app --reload --port 8001",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\""
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.0.14"
  }
}
