{
  "name": "@edusim/api-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "//": "Convenience wrapper. Canonical way to run is: uvicorn main:app --reload --port 8001",
    "dev": "uvicorn main:app --reload --port 8001 --host 0.0.0.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/express": "^4.17.21",
    "@types/pg": "^8.11.0",
    "@types/uuid": "^9.0.8"
  }
}
